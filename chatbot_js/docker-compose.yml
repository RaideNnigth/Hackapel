version: "3.8"

services:
  whatsapp-bot:
    build: .
    container_name: whatsapp-bot
    environment:
      - PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
      - PORT=3000
    ports:
      - "3000:3000"   # expõe a API HTTP
    stdin_open: true
    tty: true
