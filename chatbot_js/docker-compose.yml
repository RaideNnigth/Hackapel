version: "3.8"

services:
  whatsapp-bot:
    build: .
    container_name: whatsapp-bot
    environment:
      - PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
      - PORT=2000
    ports:
      - "2000:2000"   # expõe a API HTTP
    stdin_open: true
    tty: true
